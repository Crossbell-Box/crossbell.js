{
  "name": "crossbell",
  "version": "0.0.0",
  "description": "JavaScript SDK to interact with Crossbell",
  "keywords": [
    "crossbell",
    "eth",
    "contract"
  ],
  "license": "MIT",
  "exports": {
    "import": "./dist/index.mjs",
    "require": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run build-fast -- --dts-resolve",
    "build-fast": "tsup",
    "test": "vitest run",
    "prepublishOnly": "npm run build",
    "watch": "npm run build-fast -- --watch",
    "gen-abi-types": "abi-types-generator ./src/contracts/web3entry/abi.json --output=./src/contracts/web3entry/ --provider=ethers_v5"
  },
  "dependencies": {
    "ethers": "^5.6.3"
  },
  "devDependencies": {
    "ethereum-abi-types-generator": "^1.3.2",
    "prettier": "^2.6.0",
    "tsup": "^5.12.1",
    "typescript": "^4.6.3",
    "vitest": "^0.9.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
